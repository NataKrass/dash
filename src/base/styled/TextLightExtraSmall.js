import styled from 'styled-components';

const TextLightExtraSmall = styled.p`
  font-size: .625rem;
  font-style: normal;
  font-weight: 400;
  letter-spacing: 0rem;
  color: ${props => props.theme.colors.lightGrey};
`;

export default TextLightExtraSmall;
