import styled from 'styled-components';

const TextBlackdark = styled.p`
  font-size: .75rem;
  font-style: normal;
  font-weight: 400;
  letter-spacing: 0rem;
  color: ${props => props.theme.colors.dark};
`;

export default TextBlackdark;
